# スマブラ上達ノート（仮称: Smash Note）仕様書 v1

## 目的
大乱闘スマッシュブラザーズ SPECIAL のプレイヤーが「どのキャラ相性で負けているか」「なぜ負けたか」を記録し、
**次に何を練習すべきか**をアプリが自動で提示することで上達を加速する。
最初のユーザーは妹（すず）。誕生日プレゼント。公開OSSとして誰でも使える・直せる形にする。

## 絶対条件
- **バックエンドなし・ログインなし・個人情報を扱わない。** 全データはブラウザの localStorage のみ。
- **依存ライブラリなし**（ビルド不要の素の HTML/CSS/JS。外部 CDN・外部スクリプト・外部フォント・Analytics 一切禁止）。
- GitHub Pages でそのまま動く静的サイト（`index.html` をルートに置く）。
- 任天堂非公式。キャラ名はテキストのみ使用し、公式画像・音源・ロゴは一切同梱しない。
- 日本語UI。スマホ（幅375px）で片手操作できることを最優先。

## セキュリティ要件（みんなで見るので必須）
1. ユーザー入力を DOM に出すときは **必ず textContent か手書きの escapeHtml** を通す。`innerHTML` に未エスケープ文字列を渡す箇所をゼロにする。
2. `<meta http-equiv="Content-Security-Policy" content="default-src 'none'; script-src 'self'; style-src 'self'; img-src 'self' data:; manifest-src 'self'; base-uri 'none'; form-action 'none'; frame-ancestors 'none'">` をヘッダに入れ、インラインscript/inline style属性を使わない（CSS は外部ファイル、JS は外部ファイル）。
3. `eval` / `new Function` / `javascript:` URL / `document.write` 禁止。
4. JSON インポートは **スキーマ検証**（型・必須キー・配列長上限 10,000 件・文字列長上限 500 文字・許可キー以外は捨てる）を通してから保存。失敗時は何も上書きしない。
5. localStorage のキーは `smashnote.v1` 1本。保存前に JSON.stringify のサイズ上限（4MB）を確認。
6. `SECURITY.md`（脆弱性の報告先＝GitHub Security Advisories）、`.github/dependabot.yml`（github-actions のみ）、`.github/workflows/ci.yml`（HTML/JS の静的チェック: `grep` で innerHTML/eval/外部URL を検出したら fail、`node --check`）を用意。
7. 外部リンクは `rel="noopener noreferrer"`。

## 機能（MVP・この順で実装）
### 1. マイキャラ設定
- 89 ファイター（SSBU 全員・日本語名。ミェンミェン・カズヤ・ソラまで、ポケモントレーナーは1枠、ミィの3種は「Miiファイター（格闘/剣術/射撃）」として3枠）を `fighters.js` に配列で持つ。
- 使用キャラを複数選べる（初期はマリオ等ではなく未選択・最初に選ばせる）。

### 2. 対戦ログ（1試合10秒で入力）
- 項目: 日付(既定=今日)、自キャラ、相手キャラ、勝敗、負けた理由タグ（複数可）、一言メモ（任意・500字まで）。
- 負けた理由タグ（固定リスト）: `復帰阻止された` `着地を狩られた` `ガードが硬い/崩せない` `掴みが多い` `飛び道具に触れない` `撃墜%まで運べない` `早期撃墜された` `焦って突っ込んだ` `崖攻めが弱い` `コンボ火力負け` `ステージ不利` `その他`
- 直近の入力（自キャラ・相手キャラ）を次回の初期値にする。
- 一覧は新しい順・削除できる（削除は1回確認）。

### 3. 相性表（自キャラ × 相手キャラ）
- 対戦ログから自動集計: 対戦数・勝率。手動で「得意/普通/苦手」の印とメモも付けられる。
- 並び替え: 苦手順（勝率低い×対戦数多い）が既定。5戦未満は「まだ様子見」表示。

### 4. 上達ダッシュボード（トップ画面）
- 「今週の課題」= 直近30日の負け理由タグ上位3つ → 各タグに対応する **練習メニュー**（`drills.js` に固定で持つ。1タグに3つ、具体的に。例: 復帰阻止された → 「トレモで相手CPUを崖外に出し、崖つかまり2F/上B暴れ/回避上がりの3択を10本ずつ見る」）。
- 「次に潰す相性」= 相性表の苦手上位3。
- 直近10戦の勝率、通算勝率、連勝/連敗。
- **上達グラフ**: 週ごとの勝率を SVG で折れ線（外部ライブラリ禁止なので手書きSVG）。

### 5. データ
- JSON エクスポート（ダウンロード）／インポート（ファイル選択→スキーマ検証→確認→上書き）。
- 「全消去」は2段階確認。

## 画面構成（下タブ4つ）
`ホーム(ダッシュボード)` `記録` `相性` `設定`。上部に自キャラ切替。

## デザイン
- 白黒ベース＋アクセント1色（スマブラの赤 #E60012 に寄せた `#D7263D`）。有彩色は5%未満。文字は本文16px以上。
- 余計な装飾なし。ボタンは親指で押せる高さ44px以上。
- ダークモード対応（`prefers-color-scheme`）。

## ファイル構成
```
index.html
css/style.css
js/app.js        (状態・ルーティング・描画。モジュール分割可: js/store.js js/ui.js js/stats.js)
js/fighters.js   (89キャラ)
js/drills.js     (タグ→練習メニュー)
manifest.webmanifest (PWA最小・アイコンは同梱の単色SVG)
README.md / LICENSE(MIT) / SECURITY.md / CONTRIBUTING.md / .github/
tests/           (Node の node:test で stats.js と import 検証の単体テスト。ブラウザ不要)
```
JS は `type="module"` の ES Modules で書く。

## 完了条件
- `python3 -m http.server` で開いて全機能が動く（localStorage 永続・リロードで残る）。
- `node --test tests/` が通る。
- CI の静的チェックが通る（innerHTML/eval/外部URLゼロ）。
- 375px 幅で横スクロールなし。
- README に「使い方(3行)」「ローカルで動かす」「コントリビュート方法」「非公式表記」を書く。

## やらないこと（v1）
- クラウド同期・共有・ランキング・ログイン。
- 公式画像・音源の使用。
- オンライン対戦データの自動取得。
